[defaults]
# Flattened config - inventory in same directory
inventory = hosts.yml
host_key_checking = False
retry_files_enabled = False
deprecation_warnings = False
gathering = explicit
timeout = 30

[persistent_connection]
connect_timeout = 60
command_timeout = 60
